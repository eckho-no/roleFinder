#!/usr/bin/env bash
# Installed via `git config core.hooksPath .githooks` (scripts/setup-hooks.sh).
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
bash "$REPO_ROOT/scripts/check-privacy.sh"
